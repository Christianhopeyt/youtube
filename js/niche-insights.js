'use strict';

const NI_STRINGS = {
  EN: {
    title:'YouTube Niche Trends Dashboard', subtitle:'Analyze a recent sample of public YouTube videos to understand momentum, competition, engagement, and content opportunities.',
    input_label:'Niche, topic, or keyword', input_placeholder:'e.g. personal finance for beginners', analyze:'Analyze Niche',
    region:'Audience region', result_language:'Result relevance language', help:'Analytics use up to 50 recent relevant public videos from the last 90 days. No fake analytics are generated.',
    loading:'Collecting recent public YouTube data and calculating scores...', invalid:'Enter a niche, topic, or keyword with at least 2 characters.',
    empty:'No recent relevant public videos were found for this query.', error:'Niche analytics are temporarily unavailable. Please try again later.', api_not_configured:'YouTube data is not configured yet. No analytics were generated.', rate_limited:'The hourly analysis limit has been reached. Try again later.',
    sampled:'videos sampled', updated:'Sampled', confidence:'confidence', trend:'Trend score', competition:'Competition score', opportunity:'Opportunity score',
    avg_views:'Average views', avg_likes:'Average likes', avg_comments:'Average comments', engagement:'Average engagement', uploads:'Observed uploads/week', fast_count:'Fast performers',
    time_comparison:'7 / 30 / 90-day comparison', fast_videos:'Fast-performing videos', ai_ideas:'AI-generated ideas', keywords:'Common keywords', patterns:'Common title patterns', limitations:'Limitations',
    window:'Window', videos:'Videos', views_day:'Median views/day', engagement_short:'Engagement', uploads_short:'Uploads/week',
    youtube_source:'Analytics and scores use sampled public YouTube Data API fields.', ai_source:'Summary and content ideas are AI-generated by Gemini and do not change analytics.', stale:'Showing an older cached YouTube sample because live data is temporarily unavailable.',
    summary:'Niche summary', titles:'Title ideas', tags:'Tag suggestions', angles:'Video angle ideas', ai_unavailable:'AI suggestions are unavailable. Public YouTube analytics remain valid.',
    no_fast:'No fast-performing videos met the current sample threshold.', low:'low', medium:'medium', high:'high', views_per_day:'views/day', days_old:'days old',
    pattern_how_to:'How-to', pattern_numbered:'List / numbered', pattern_question:'Question', pattern_beginner:'Beginner', pattern_comparison:'Comparison', pattern_review:'Review', pattern_urgency:'Urgency / newness',
    guide_title:'How to Use Niche Insights', guide_intro:'Discover trending YouTube niches and content opportunities in minutes.',
    guide_step1_title:'Enter a niche or keyword', guide_step1_text:'Example: Fitness, Christian Motivation, AI Tools.',
    guide_step2_title:'Review the niche analytics', guide_step2_text:'Compare the trend, competition, and opportunity scores.',
    guide_step3_title:'Analyze content opportunities', guide_step3_text:'Review keywords, fast-growing videos, and signs of audience demand.',
    guide_step4_title:'Generate content ideas', guide_step4_text:'Use AI-powered title ideas, tags, and video angles as creative starting points.',
    guide_step5_title:'Create and validate your next video', guide_step5_text:'Use the insights to choose and validate high-potential content topics.',
    limit_sample:'The report analyzes a sample of up to 50 recent relevant public videos, not every video in the niche.',
    limit_directional:'Scores are directional indicators based on sampled public data and do not guarantee performance.',
    limit_frequency:'Upload frequency means observed relevant uploads in the sample.',
    limit_unavailable:'Likes, comments, and tags may be unavailable for some videos.',
    window_label:'Analysis window', recent_title:'Recent searches', recent_clear:'Clear',
    share_btn:'Copy report', share_copied:'Report copied to clipboard.', share_failed:'Copy failed. Select the text and copy manually.'
  },
  FR: {
    guide_title:'Comment Utiliser Niche Insights', guide_intro:'D\u00e9couvrez les niches YouTube tendance et les opportunit\u00e9s de contenu en quelques minutes.',
    guide_step1_title:'Saisissez une niche ou un mot-cl\u00e9', guide_step1_text:'Exemple : Fitness, Motivation Chr\u00e9tienne, Outils IA.',
    guide_step2_title:'Examinez les statistiques de la niche', guide_step2_text:'Comparez les scores de tendance, de concurrence et d\u2019opportunit\u00e9.',
    guide_step3_title:'Analysez les opportunit\u00e9s de contenu', guide_step3_text:'Examinez les mots-cl\u00e9s, les vid\u00e9os en forte croissance et les signes de demande du public.',
    guide_step4_title:'G\u00e9n\u00e9rez des id\u00e9es de contenu', guide_step4_text:'Utilisez les id\u00e9es de titres, tags et angles de vid\u00e9os g\u00e9n\u00e9r\u00e9s par IA comme points de d\u00e9part.',
    guide_step5_title:'Cr\u00e9ez et validez votre prochaine vid\u00e9o', guide_step5_text:'Utilisez les informations pour choisir et valider des sujets de contenu \u00e0 fort potentiel.',
    title:'Tableau des Tendances de Niches YouTube', subtitle:'Analysez un échantillon récent de vidéos YouTube publiques pour comprendre la dynamique, la concurrence, l’engagement et les opportunités.',
    input_label:'Niche, sujet ou mot-clé', input_placeholder:'ex. finances personnelles pour débutants', analyze:'Analyser la Niche',
    region:'Région de l’audience', result_language:'Langue de pertinence des résultats', help:'Les statistiques utilisent jusqu’à 50 vidéos publiques récentes des 90 derniers jours. Aucune statistique fictive n’est générée.',
    loading:'Collecte des données YouTube publiques récentes et calcul des scores...', invalid:'Saisissez une niche, un sujet ou un mot-clé d’au moins 2 caractères.',
    empty:'Aucune vidéo publique récente et pertinente n’a été trouvée.', error:'Les statistiques de niche sont temporairement indisponibles. Réessayez plus tard.',
    api_not_configured:'Les données YouTube ne sont pas encore configurées. Aucune statistique n’a été générée.', rate_limited:'La limite horaire d’analyses a été atteinte. Réessayez plus tard.',
    sampled:'vidéos échantillonnées', updated:'Échantillonné le', confidence:'confiance', trend:'Score de tendance', competition:'Score de concurrence', opportunity:'Score d’opportunité',
    avg_views:'Vues moyennes', avg_likes:'Likes moyens', avg_comments:'Commentaires moyens', engagement:'Engagement moyen', uploads:'Publications observées/semaine', fast_count:'Vidéos rapides',
    time_comparison:'Comparaison sur 7 / 30 / 90 jours', fast_videos:'Vidéos à progression rapide', ai_ideas:'Idées générées par IA', keywords:'Mots-clés fréquents', patterns:'Modèles de titres fréquents', limitations:'Limites',
    window:'Période', videos:'Vidéos', views_day:'Vues médianes/jour', engagement_short:'Engagement', uploads_short:'Publications/semaine',
    youtube_source:'Les statistiques et scores utilisent un échantillon de données publiques de l’API YouTube.', ai_source:'Le résumé et les idées sont générés par Gemini et ne modifient pas les statistiques.', stale:'Affichage d’un ancien échantillon YouTube mis en cache, car les données en direct sont indisponibles.',
    summary:'Résumé de la niche', titles:'Idées de titres', tags:'Suggestions de tags', angles:'Angles de vidéos', ai_unavailable:'Les suggestions IA sont indisponibles. Les statistiques YouTube publiques restent valides.',
    no_fast:'Aucune vidéo à progression rapide n’a atteint le seuil de l’échantillon.', low:'faible', medium:'moyenne', high:'élevée', views_per_day:'vues/jour', days_old:'jours',
    pattern_how_to:'Tutoriel', pattern_numbered:'Liste / numéroté', pattern_question:'Question', pattern_beginner:'Débutant', pattern_comparison:'Comparaison', pattern_review:'Avis / test', pattern_urgency:'Urgence / nouveauté',
    limit_sample:'Le rapport analyse un échantillon de 50 vidéos publiques récentes et pertinentes au maximum, et non toutes les vidéos de la niche.',
    limit_directional:'Les scores sont des indicateurs directionnels basés sur les données publiques échantillonnées et ne garantissent aucun résultat.',
    limit_frequency:'La fréquence de publication correspond aux publications pertinentes observées dans l’échantillon.',
    limit_unavailable:'Les likes, commentaires et tags peuvent être indisponibles pour certaines vidéos.',
    window_label:'Période d’analyse', recent_title:'Recherches récentes', recent_clear:'Effacer',
    share_btn:'Copier le rapport', share_copied:'Rapport copié dans le presse-papiers.', share_failed:'Échec de la copie. Sélectionnez le texte et copiez-le manuellement.'
  }
};

const NicheInsights = {
  lang: 'EN',
  data: null,
  analytics: null,
  windowKey: '90',
  init() {
    this.form = document.getElementById('niche-form');
    if (!this.form) return;
    this.lang = localStorage.getItem('yta-lang') === 'FR' ? 'FR' : 'EN';
    this.applyLanguage();
    this.form.addEventListener('submit', event => { event.preventDefault(); this.run(); });
    document.querySelectorAll('.lang-btn').forEach(button => button.addEventListener('click', () => {
      setTimeout(() => { this.lang = button.dataset.lang; this.applyLanguage(); this.renderRecent(); if (this.data) this.render(this.data); }, 0);
    }));
    const select = document.getElementById('niche-window-select');
    if (select) select.querySelectorAll('button').forEach(button => button.addEventListener('click', () => {
      this.windowKey = button.dataset.window;
      select.querySelectorAll('button').forEach(item => item.setAttribute('aria-pressed', String(item === button)));
      if (this.analytics) this.renderMetrics(this.analytics);
    }));
    const share = document.getElementById('niche-share-btn');
    if (share) share.addEventListener('click', () => this.shareReport());
    this.renderRecent();
  },
  text(key) { return NI_STRINGS[this.lang]?.[key] || NI_STRINGS.EN[key] || ''; },
  applyLanguage() {
    document.querySelectorAll('[data-ni]').forEach(element => {
      const value = this.text(element.dataset.ni);
      if (value) element.textContent = value;
    });
    document.querySelectorAll('[data-ni-placeholder]').forEach(element => {
      const value = this.text(element.dataset.niPlaceholder);
      if (value) element.placeholder = value;
    });
    document.getElementById('niche-language').value = this.lang === 'FR' ? 'fr' : 'en';
  },
  setStatus(message, error = false, loading = false) {
    const status = document.getElementById('niche-status');
    status.textContent = message;
    status.classList.toggle('error', error);
    status.classList.toggle('loading', loading);
  },
  async run() {
    const query = document.getElementById('niche-query').value.trim();
    if (query.length < 2) { this.setStatus(this.text('invalid'), true); document.getElementById('niche-query').focus(); return; }
    const region = document.getElementById('niche-region').value;
    const language = document.getElementById('niche-language').value;
    const button = document.getElementById('niche-submit');
    button.disabled = true;
    this.setStatus(this.text('loading'), false, true);
    try {
      const response = await fetch('/api/niche-insights', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({
          query,
          region,
          language,
          includeAiSuggestions: true
        })
      });
      const data = await response.json();
      if (!response.ok) {
        const errorKey = data.code === 'API_NOT_CONFIGURED' ? 'api_not_configured'
          : data.code === 'RATE_LIMIT' ? 'rate_limited'
          : 'error';
        throw new Error(this.text(errorKey));
      }
      this.data = data;
      this.analytics = data.analytics;
      this.saveRecent({ query, region, language });
      this.render(data);
      this.setStatus('');
    } catch (error) {
      this.setStatus(error.message || this.text('error'), true);
    } finally {
      button.disabled = false;
    }
  },
  number(value, digits = 0) {
    if (value === null || value === undefined || !Number.isFinite(Number(value))) return '—';
    return new Intl.NumberFormat(this.lang === 'FR' ? 'fr-FR' : 'en-US', { maximumFractionDigits: digits }).format(value);
  },
  percent(value) { return value === null || value === undefined ? '—' : `${this.number(value, 2)}%`; },
  escape(value) { const div = document.createElement('div'); div.textContent = String(value ?? ''); return div.innerHTML; },
  scoreCard(kind, label, value) {
    return `<article class="niche-score-card ${kind}"><div class="niche-score-label">${this.escape(label)}</div><div class="niche-score-value">${value}</div><div class="niche-score-bar"><div class="niche-score-fill" style="width:${value}%"></div></div></article>`;
  },
  render(data) {
    const analytics = data.analytics;
    const results = document.getElementById('niche-results');
    results.classList.add('visible');
    document.getElementById('report-title').textContent = data.query;
    document.getElementById('report-meta').textContent = `${analytics.sampleSize} ${this.text('sampled')} · ${this.text('updated')} ${new Date(data.sampledAt).toLocaleString(this.lang === 'FR' ? 'fr-FR' : 'en-US')}`;
    document.getElementById('confidence-badge').textContent = `${this.text(analytics.confidence)} ${this.text('confidence')}`;
    this.renderNotices(data);
    if (!analytics.sampleSize) {
      document.getElementById('score-grid').innerHTML = '';
      document.getElementById('metrics-grid').innerHTML = `<div class="niche-empty niche-empty-wide">${this.escape(this.text('empty'))}</div>`;
      document.querySelector('.niche-layout').hidden = true;
      this.setStatus(this.text('empty'), true);
      return;
    }
    document.querySelector('.niche-layout').hidden = false;
    document.getElementById('score-grid').innerHTML = [
      this.scoreCard('trend', this.text('trend'), analytics.scores.trend),
      this.scoreCard('competition', this.text('competition'), analytics.scores.competition),
      this.scoreCard('opportunity', this.text('opportunity'), analytics.scores.opportunity)
    ].join('');
    const select = document.getElementById('niche-window-select');
    if (select) select.hidden = false;
    this.renderMetrics(analytics);
    this.renderWindows(analytics.windows);
    this.renderKeywords(analytics.commonKeywords);
    this.renderPatterns(analytics.titlePatterns);
    this.renderFastVideos(analytics.fastPerformers);
    this.renderAi(data.aiSuggestions, data.aiError);
    document.getElementById('limitations-list').innerHTML = data.limitations.map(item => `<li>${this.escape(this.text(`limit_${item}`))}</li>`).join('');
    results.scrollIntoView({ behavior: matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth', block: 'start' });
  },
  renderNotices(data) {
    const notices = [`<div class="source-notice youtube">${this.escape(this.text('youtube_source'))}</div>`];
    if (data.source?.suggestions) notices.push(`<div class="source-notice ai">${this.escape(this.text('ai_source'))}</div>`);
    if (data.source?.stale) notices.push(`<div class="source-notice warning">${this.escape(this.text('stale'))}</div>`);
    document.getElementById('source-notices').innerHTML = notices.join('');
  },
  renderMetrics(analytics) {
    const w = analytics.windows[this.windowKey] || analytics.windows['90'];
    const items = [
      [this.text('avg_views'), this.number(w.averageViews)],
      [this.text('avg_likes'), this.number(w.averageLikes)],
      [this.text('avg_comments'), this.number(w.averageComments)],
      [this.text('engagement'), this.percent(w.averageEngagementRate)],
      [this.text('uploads'), this.number(w.uploadsPerWeek, 1)],
      [this.text('fast_count'), this.number(analytics.fastPerformers.length)]
    ];
    document.getElementById('metrics-grid').innerHTML = items.map(([label,value]) => `<article class="niche-metric-card"><div class="niche-metric-value">${value}</div><div class="niche-metric-label">${this.escape(label)}</div></article>`).join('');
  },
  renderWindows(windows) {
    const rows = ['7','30','90'].map(days => {
      const w = windows[days];
      return `<tr><td>${days}d</td><td>${w.videoCount}</td><td>${this.number(w.medianViewsPerDay)}</td><td>${this.percent(w.medianEngagementRate)}</td><td>${this.number(w.uploadsPerWeek,1)}</td></tr>`;
    }).join('');
    document.getElementById('window-table').innerHTML = `<table class="window-table"><thead><tr><th>${this.text('window')}</th><th>${this.text('videos')}</th><th>${this.text('views_day')}</th><th>${this.text('engagement_short')}</th><th>${this.text('uploads_short')}</th></tr></thead><tbody>${rows}</tbody></table>`;
  },
  renderKeywords(keywords) {
    document.getElementById('keyword-list').innerHTML = keywords.length
      ? keywords.map(item => `<span class="keyword-chip">${this.escape(item.keyword)} <small>${item.count}</small></span>`).join('')
      : '—';
  },
  renderPatterns(patterns) {
    document.getElementById('pattern-list').innerHTML = patterns.length
      ? patterns.map(item => `<div class="pattern-item">${this.escape(this.text(`pattern_${item.pattern}`))} · ${item.count}</div>`).join('')
      : '—';
  },
  renderFastVideos(videos) {
    document.getElementById('fast-videos').innerHTML = videos.length ? videos.map(video =>
      `<a class="fast-video" href="${this.escape(video.url)}" target="_blank" rel="noopener"><img src="${this.escape(video.thumbnail)}" alt="" loading="lazy"><div><div class="fast-video-title">${this.escape(video.title)}</div><div class="fast-video-meta">${this.escape(video.channelTitle)} · ${this.number(video.viewsPerDay)} ${this.text('views_per_day')} · ${this.number(video.ageDays,0)} ${this.text('days_old')}</div></div></a>`
    ).join('') : `<div class="niche-empty">${this.escape(this.text('no_fast'))}</div>`;
  },
  renderAi(ai, error) {
    const container = document.getElementById('ai-content');
    if (!ai) { container.innerHTML = `<div class="niche-empty">${this.escape(error || this.text('ai_unavailable'))}</div>`; return; }
    const list = (title, items) => `<div class="ai-group"><h4>${this.escape(title)}</h4><div class="idea-list">${items.map(item => `<div class="idea-item">${this.escape(item)}</div>`).join('')}</div></div>`;
    container.innerHTML = `<h4>${this.escape(this.text('summary'))}</h4><p class="ai-summary">${this.escape(ai.summary)}</p>${list(this.text('titles'),ai.titleIdeas)}${list(this.text('tags'),ai.tagSuggestions)}${list(this.text('angles'),ai.videoAngles)}`;
  },
  saveRecent({ query, region, language }) {
    try {
      const current = JSON.parse(localStorage.getItem('ni-recent') || '[]');
      const next = [{ query, region, language }, ...current.filter(item => !(item.query === query && item.region === region && item.language === language))].slice(0, 5);
      localStorage.setItem('ni-recent', JSON.stringify(next));
      this.renderRecent();
    } catch (_) {}
  },
  renderRecent() {
    const container = document.getElementById('niche-recent');
    if (!container) return;
    let items = [];
    try { items = JSON.parse(localStorage.getItem('ni-recent') || '[]'); } catch (_) {}
    if (!items.length) { container.hidden = true; return; }
    container.hidden = false;
    container.innerHTML = `<span class="niche-recent-title">${this.escape(this.text('recent_title'))}</span>${items.map(item => `<button type="button" class="niche-recent-chip" data-q="${this.escape(item.query)}" data-r="${this.escape(item.region)}" data-l="${this.escape(item.language)}">${this.escape(item.query)}</button>`).join('')}<button type="button" class="niche-recent-clear" id="niche-recent-clear">${this.escape(this.text('recent_clear'))}</button>`;
    container.querySelectorAll('.niche-recent-chip').forEach(chip => chip.addEventListener('click', () => {
      document.getElementById('niche-query').value = chip.dataset.q;
      document.getElementById('niche-region').value = chip.dataset.r;
      document.getElementById('niche-language').value = chip.dataset.l;
      this.run();
    }));
    const clear = document.getElementById('niche-recent-clear');
    if (clear) clear.addEventListener('click', () => {
      try { localStorage.removeItem('ni-recent'); } catch (_) {}
      this.renderRecent();
    });
  },
  reportText() {
    const data = this.data;
    if (!data) return '';
    const analytics = data.analytics;
    const w = analytics.windows['90'];
    const scores = analytics.scores;
    const top = analytics.fastPerformers[0];
    const lines = [
      `Norlytics Niche Insights: ${data.query}`,
      `${this.text('region')}: ${data.region} | ${this.text('result_language')}: ${data.language}`,
      `${this.text('sampled')}: ${analytics.sampleSize} | ${this.text('updated')}: ${new Date(data.sampledAt).toLocaleString(this.lang === 'FR' ? 'fr-FR' : 'en-US')}`,
      `${this.text('trend')}: ${scores.trend} | ${this.text('competition')}: ${scores.competition} | ${this.text('opportunity')}: ${scores.opportunity}`,
      `${this.text('avg_views')}: ${this.number(w.averageViews)} | ${this.text('engagement')}: ${this.percent(w.averageEngagementRate)} | ${this.text('uploads')}: ${this.number(w.uploadsPerWeek, 1)}`,
      analytics.commonKeywords.length ? `${this.text('keywords')}: ${analytics.commonKeywords.slice(0, 6).map(item => item.keyword).join(', ')}` : '',
      top ? `${this.text('fast_count')}: ${top.title} (${this.number(top.viewsPerDay)} ${this.text('views_per_day')})` : '',
      'https://norcanto.com/niche-insights'
    ].filter(Boolean);
    return lines.join('\n');
  },
  shareReport() {
    const button = document.getElementById('niche-share-btn');
    if (!button || !this.data) return;
    const restore = () => { button.textContent = this.text('share_btn'); button.disabled = false; };
    const feedback = ok => { button.textContent = this.text(ok ? 'share_copied' : 'share_failed'); setTimeout(restore, 2200); };
    const text = this.reportText();
    button.disabled = true;
    if (navigator.clipboard && window.isSecureContext) {
      navigator.clipboard.writeText(text).then(() => feedback(true)).catch(() => feedback(false));
    } else {
      const textarea = document.createElement('textarea');
      textarea.value = text;
      textarea.style.position = 'fixed';
      textarea.style.opacity = '0';
      document.body.appendChild(textarea);
      textarea.select();
      let ok = false;
      try { ok = document.execCommand('copy'); } catch (_) {}
      document.body.removeChild(textarea);
      feedback(ok);
    }
  }
};

document.addEventListener('DOMContentLoaded', () => NicheInsights.init());
